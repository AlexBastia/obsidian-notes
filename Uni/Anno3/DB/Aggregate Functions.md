Compute values form a set of tuples using functions. Not really adapt to complex functions because the languages wasn't made for that shii.

An example is the COUNT function, first the SELECT is calculated, then the count is performed.

## Grouping
