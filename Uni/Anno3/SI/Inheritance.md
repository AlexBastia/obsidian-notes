To define a class that is an *extension* of other classes, with new and/or specialized attributes and methods (eg. Dog and Cat could both inherit the class Animal while being two distinct classes themselves).
	Note: a class can inherit from *multiple* different classes, this is called **Multiple Inheritance**.

It's represented with an upside down triangle.