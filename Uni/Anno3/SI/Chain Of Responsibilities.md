Es -> a URL is opened on a phone:
- Does an app exist to handle this type of input?
- If yes, use that. Else, go to the browser.

There are many different classes that can be used to answer a certain need, and each one points to the next using an if close to check if the current class can execute the request, otherwise it passes it on to the next 'node' in the 'chain'.2