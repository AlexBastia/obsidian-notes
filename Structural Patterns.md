These patterns are concerned with how structures are formed by the composition of classes and objects.

Two types of structural patterns:
- **Structural class pattern**: uses inheritance to compose interfaces or implementations.
- **Structural object pattern**: describes the ways to compose objects to realize new functionality.

Structural Patterns: Adapter, Bridge, Composite ,
Decorator, Facade, Flyweight, Proxy.